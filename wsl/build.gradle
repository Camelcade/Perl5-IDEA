sourceSets {
  main {
    java.srcDirs 'src'
    resources.srcDir 'resources'
  }
  test {
    java.srcDir 'test'
    resources.srcDirs 'testData'
  }
}

intellij{
  type 'IU'
  plugins = [project(':perl5.core'), 'remote-run']
}

dependencies{
  compileOnly project(":perl5.core")
}