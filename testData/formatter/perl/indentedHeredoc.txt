<<~`EOM`;
    one liner $force_leaf
EOM

<<~`EOM`;
        one liner $force_leaf
EOM

<<~`EOM`;
    one liner $force_leaf
EOM

<<~`EOM`;
    one liner $force_leaf
EOM

<<~`EOM`;
one liner $force_leaf
EOM

<<~`EOM`;
        one liner $force_leaf
EOM


<<~`EOM`;
    one liner $force_leaf
        multi liner
EOM

<<~`EOM`;
        one liner $force_leaf
            multi liner
EOM

<<~`EOM`;
    one liner $force_leaf
        multi liner
EOM

<<~`EOM`;
    one liner $force_leaf
        multi liner
EOM

<<~`EOM`;
one liner $force_leaf
    multi liner
EOM

<<~`EOM`;
        one liner $force_leaf
            multi liner
EOM


{
    <<~`EOM`;
        one liner $force_leaf
    EOM

    <<~`EOM`;
            one liner $force_leaf
    EOM

    <<~`EOM`;
        one liner $force_leaf
    EOM

    <<~`EOM`;
        one liner $force_leaf
    EOM

    <<~`EOM`;
    one liner $force_leaf
    EOM

    <<~`EOM`;
            one liner $force_leaf
    EOM


    <<~`EOM`;
        one liner $force_leaf
            multi liner
    EOM

    <<~`EOM`;
            one liner $force_leaf
                multi liner
    EOM

    <<~`EOM`;
        one liner $force_leaf
            multi liner
    EOM

    <<~`EOM`;
        one liner $force_leaf
            multi liner
    EOM

    <<~`EOM`;
    one liner $force_leaf
        multi liner
    EOM

    <<~`EOM`;
            one liner $force_leaf
                multi liner
    EOM
}

{
    <<~`EOM`;
        one liner $force_leaf
    EOM

    <<~`EOM`;
            one liner $force_leaf
    EOM

    <<~`EOM`;
        one liner $force_leaf
    EOM

    <<~`EOM`;
        one liner $force_leaf
    EOM

    <<~`EOM`;
    one liner $force_leaf
    EOM

    <<~`EOM`;
            one liner $force_leaf
    EOM


    <<~`EOM`;
        one liner $force_leaf
            multi liner
    EOM

    <<~`EOM`;
            one liner $force_leaf
                multi liner
    EOM

    <<~`EOM`;
        one liner $force_leaf
            multi liner
    EOM

    <<~`EOM`;
        one liner $force_leaf
            multi liner
    EOM

    <<~`EOM`;
    one liner $force_leaf
        multi liner
    EOM

    <<~`EOM`;
            one liner $force_leaf
                multi liner
    EOM
}