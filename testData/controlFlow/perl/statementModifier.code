
say $a = $b if $_;      # first
say $a = $b unless $_;  # first
say $a = $b when $_;    # first

say $a = $b while $_;   # first
say $a = $b until $_;   # first

say $a = $b for @_;     # first
say $a = $b foreach @_; # first
