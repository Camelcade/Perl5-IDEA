Matching value:
	[--prompt]
Completion items:
	[--auto-cleanup <days>]; [Number of days that cpanm's work directories expire in. Defaults to 7]; /perl5language.png; [cpanm --prompt --auto-cleanup <days>]
	[--force]; [Force install]; /perl5language.png; [cpanm --prompt --force]
	[--from <url>]; [Use only this mirror base URL and its index file]; /perl5language.png; [cpanm --prompt --from <url>]
	[--installdeps]; [Only install dependencies]; /perl5language.png; [cpanm --prompt --installdeps]
	[--interactive]; [Turns on interactive configure (required for Task:: modules)]; /perl5language.png; [cpanm --prompt --interactive]
	[--local-lib <path>]; [Specify the install base to install modules]; /perl5language.png; [cpanm --prompt --local-lib <path>]
	[--local-lib-contained <path>]; [Specify the install base to install all non-core modules]; /perl5language.png; [cpanm --prompt --local-lib-contained <path>]
	[--mirror <url>]; [Specify the base URL for the mirror (e.g. https://cpan.cpantesters.org/)]; /perl5language.png; [cpanm --prompt --mirror <url>]
	[--mirror-only <url>]; [Use the mirror's index file instead of the CPAN Meta DB]; /perl5language.png; [cpanm --prompt --mirror-only <url>]
	[--notest]; [Do not run unit tests]; /perl5language.png; [cpanm --prompt --notest]
	[--quiet]; [Turns off the most output]; /perl5language.png; [cpanm --prompt --quiet]
	[--reinstall]; [Reinstall the distribution even if you already have the latest version installed]; /perl5language.png; [cpanm --prompt --reinstall]
	[--self-contained]; [Install all non-core modules, even if they're already installed.]; /perl5language.png; [cpanm --prompt --self-contained]
	[--showdeps]; [Only display direct dependencies]; /perl5language.png; [cpanm --prompt --showdeps]
	[--sudo]; [sudo to run install commands]; /perl5language.png; [cpanm --prompt --sudo]
	[--test-only]; [Run tests only, do not install]; /perl5language.png; [cpanm --prompt --test-only]
	[--verbose]; [Turns on chatty output]; /perl5language.png; [cpanm --prompt --verbose]
	[ <modules>]; [Install perl modules by name, path or url]; /perl5language.png; [cpanm --prompt <modules>]
