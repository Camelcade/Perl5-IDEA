Matching value:
	[--quiet]
Completion items:
	[--auto-cleanup <days>]; [Number of days that cpanm's work directories expire in. Defaults to 7]; /perl5language.png; [cpanm --quiet --auto-cleanup <days>]
	[--force]; [Force install]; /perl5language.png; [cpanm --quiet --force]
	[--from <url>]; [Use only this mirror base URL and its index file]; /perl5language.png; [cpanm --quiet --from <url>]
	[--info <modules>]; [Displays distribution info on CPAN]; /perl5language.png; [cpanm --quiet --info <modules>]
	[--installdeps]; [Only install dependencies]; /perl5language.png; [cpanm --quiet --installdeps]
	[--interactive]; [Turns on interactive configure (required for Task:: modules)]; /perl5language.png; [cpanm --quiet --interactive]
	[--local-lib <path>]; [Specify the install base to install modules]; /perl5language.png; [cpanm --quiet --local-lib <path>]
	[--local-lib-contained <path>]; [Specify the install base to install all non-core modules]; /perl5language.png; [cpanm --quiet --local-lib-contained <path>]
	[--look <module>]; [Opens the distribution with your SHELL]; /perl5language.png; [cpanm --quiet --look <module>]
	[--mirror <url>]; [Specify the base URL for the mirror (e.g. https://cpan.cpantesters.org/)]; /perl5language.png; [cpanm --quiet --mirror <url>]
	[--mirror-only <url>]; [Use the mirror's index file instead of the CPAN Meta DB]; /perl5language.png; [cpanm --quiet --mirror-only <url>]
	[--notest]; [Do not run unit tests]; /perl5language.png; [cpanm --quiet --notest]
	[--prompt]; [Prompt when configure/build/test fails]; /perl5language.png; [cpanm --quiet --prompt]
	[--reinstall]; [Reinstall the distribution even if you already have the latest version installed]; /perl5language.png; [cpanm --quiet --reinstall]
	[--self-contained]; [Install all non-core modules, even if they're already installed.]; /perl5language.png; [cpanm --quiet --self-contained]
	[--self-upgrade]; [Upgrades itself]; /perl5language.png; [cpanm --quiet --self-upgrade]
	[--showdeps]; [Only display direct dependencies]; /perl5language.png; [cpanm --quiet --showdeps]
	[--sudo]; [sudo to run install commands]; /perl5language.png; [cpanm --quiet --sudo]
	[--test-only]; [Run tests only, do not install]; /perl5language.png; [cpanm --quiet --test-only]
	[--uninstall <modules>]; [Uninstalls the modules (EXPERIMENTAL)]; /perl5language.png; [cpanm --quiet --uninstall <modules>]
	[--verbose]; [Turns on chatty output]; /perl5language.png; [cpanm --quiet --verbose]
	[ <modules>]; [Install perl modules by name, path or url]; /perl5language.png; [cpanm --quiet <modules>]
