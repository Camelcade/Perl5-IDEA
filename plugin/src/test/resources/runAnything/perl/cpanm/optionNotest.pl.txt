Matching value:
	[--notest]
Completion items:
	[--auto-cleanup <days>]; [Number of days that cpanm's work directories expire in. Defaults to 7]; /perl5language.png; [cpanm --notest --auto-cleanup <days>]
	[--force]; [Force install]; /perl5language.png; [cpanm --notest --force]
	[--from <url>]; [Use only this mirror base URL and its index file]; /perl5language.png; [cpanm --notest --from <url>]
	[--installdeps]; [Only install dependencies]; /perl5language.png; [cpanm --notest --installdeps]
	[--interactive]; [Turns on interactive configure (required for Task:: modules)]; /perl5language.png; [cpanm --notest --interactive]
	[--local-lib <path>]; [Specify the install base to install modules]; /perl5language.png; [cpanm --notest --local-lib <path>]
	[--local-lib-contained <path>]; [Specify the install base to install all non-core modules]; /perl5language.png; [cpanm --notest --local-lib-contained <path>]
	[--mirror <url>]; [Specify the base URL for the mirror (e.g. http://cpan.cpantesters.org/)]; /perl5language.png; [cpanm --notest --mirror <url>]
	[--mirror-only <url>]; [Use the mirror's index file instead of the CPAN Meta DB]; /perl5language.png; [cpanm --notest --mirror-only <url>]
	[--prompt]; [Prompt when configure/build/test fails]; /perl5language.png; [cpanm --notest --prompt]
	[--quiet]; [Turns off the most output]; /perl5language.png; [cpanm --notest --quiet]
	[--reinstall]; [Reinstall the distribution even if you already have the latest version installed]; /perl5language.png; [cpanm --notest --reinstall]
	[--self-contained]; [Install all non-core modules, even if they're already installed.]; /perl5language.png; [cpanm --notest --self-contained]
	[--showdeps]; [Only display direct dependencies]; /perl5language.png; [cpanm --notest --showdeps]
	[--sudo]; [sudo to run install commands]; /perl5language.png; [cpanm --notest --sudo]
	[--test-only]; [Run tests only, do not install]; /perl5language.png; [cpanm --notest --test-only]
	[--verbose]; [Turns on chatty output]; /perl5language.png; [cpanm --notest --verbose]
	[ <modules>]; [Install perl modules by name, path or url]; /perl5language.png; [cpanm --notest <modules>]
