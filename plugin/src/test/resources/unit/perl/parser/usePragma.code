use attributes;
use autodie;
use autodie::exception;
use autodie::exception::system;
use autodie::hints;
use autodie::skip;
use autouse;
use base;
use bigfloat;
use bigint;
use bignum;
use bigrat;
use blib;
use builtin;
use bytes;
use charnames;
use constant;
use deprecate;
use diagnostics;
use encoding;
use encoding::warnings;
use experimental;
use feature;
use fields;
use filetest;
use if;
use integer;
use less;
use lib;
use locale;
use mro;
use ok;
use open;
use ops;
use overload;
use overloading;
use parent;
use re;
use sigtrap;
use sort;
use stable;
use strict;
use subs;
use threads;
use threads::shared;
use utf8;
use vars;
use version;
use vmsish;
use warnings;
use warnings::register;