sub     name :lvalue :prototype($) ($s<caret>ig) { # with signature, prototype
}
######################### PREVIEW #########################
sub     name :lvalue :prototype($) ($sig) { # with signature, prototype
}