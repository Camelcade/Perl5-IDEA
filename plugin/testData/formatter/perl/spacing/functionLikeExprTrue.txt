grep( $something );
map( $something );
sort( $something );
say( $something );
print( $something );
printf( $something );
require($something);
undef($something);
shift( $something );
unshift( $something );
push( $something );
pop( $something );
scalar( $something );
keys( $something );
values( $something );
each( $something );
defined( $something );
wantarray( $something );
delete( $something );
splice( $something );
bless( $something );
some_sub( $something );
return ($something);