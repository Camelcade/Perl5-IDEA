Matching value:
	[--force]
Completion items:
	[--auto-cleanup <days>]; [Number of days that cpanm's work directories expire in. Defaults to 7]; /perl5language.png; [cpanm --force --auto-cleanup <days>]
	[--from <url>]; [Use only this mirror base URL and its index file]; /perl5language.png; [cpanm --force --from <url>]
	[--installdeps]; [Only install dependencies]; /perl5language.png; [cpanm --force --installdeps]
	[--interactive]; [Turns on interactive configure (required for Task:: modules)]; /perl5language.png; [cpanm --force --interactive]
	[--local-lib <path>]; [Specify the install base to install modules]; /perl5language.png; [cpanm --force --local-lib <path>]
	[--local-lib-contained <path>]; [Specify the install base to install all non-core modules]; /perl5language.png; [cpanm --force --local-lib-contained <path>]
	[--mirror <url>]; [Specify the base URL for the mirror (e.g. http://cpan.cpantesters.org/)]; /perl5language.png; [cpanm --force --mirror <url>]
	[--mirror-only <url>]; [Use the mirror's index file instead of the CPAN Meta DB]; /perl5language.png; [cpanm --force --mirror-only <url>]
	[--notest]; [Do not run unit tests]; /perl5language.png; [cpanm --force --notest]
	[--prompt]; [Prompt when configure/build/test fails]; /perl5language.png; [cpanm --force --prompt]
	[--quiet]; [Turns off the most output]; /perl5language.png; [cpanm --force --quiet]
	[--reinstall]; [Reinstall the distribution even if you already have the latest version installed]; /perl5language.png; [cpanm --force --reinstall]
	[--self-contained]; [Install all non-core modules, even if they're already installed.]; /perl5language.png; [cpanm --force --self-contained]
	[--showdeps]; [Only display direct dependencies]; /perl5language.png; [cpanm --force --showdeps]
	[--sudo]; [sudo to run install commands]; /perl5language.png; [cpanm --force --sudo]
	[--test-only]; [Run tests only, do not install]; /perl5language.png; [cpanm --force --test-only]
	[--verbose]; [Turns on chatty output]; /perl5language.png; [cpanm --force --verbose]
	[ <modules>]; [Install perl modules by name, path or url]; /perl5language.png; [cpanm --force <modules>]
