Matching value:
	[--test-only]
Completion items:
	[--auto-cleanup <days>]; [Number of days that cpanm's work directories expire in. Defaults to 7]; /icons/perl5language.png; [cpanm --test-only --auto-cleanup <days>]
	[--force]; [Force install]; /icons/perl5language.png; [cpanm --test-only --force]
	[--from <url>]; [Use only this mirror base URL and its index file]; /icons/perl5language.png; [cpanm --test-only --from <url>]
	[--installdeps]; [Only install dependencies]; /icons/perl5language.png; [cpanm --test-only --installdeps]
	[--interactive]; [Turns on interactive configure (required for Task:: modules)]; /icons/perl5language.png; [cpanm --test-only --interactive]
	[--local-lib <path>]; [Specify the install base to install modules]; /icons/perl5language.png; [cpanm --test-only --local-lib <path>]
	[--local-lib-contained <path>]; [Specify the install base to install all non-core modules]; /icons/perl5language.png; [cpanm --test-only --local-lib-contained <path>]
	[--mirror <url>]; [Specify the base URL for the mirror (e.g. http://cpan.cpantesters.org/)]; /icons/perl5language.png; [cpanm --test-only --mirror <url>]
	[--mirror-only <url>]; [Use the mirror's index file instead of the CPAN Meta DB]; /icons/perl5language.png; [cpanm --test-only --mirror-only <url>]
	[--notest]; [Do not run unit tests]; /icons/perl5language.png; [cpanm --test-only --notest]
	[--prompt]; [Prompt when configure/build/test fails]; /icons/perl5language.png; [cpanm --test-only --prompt]
	[--quiet]; [Turns off the most output]; /icons/perl5language.png; [cpanm --test-only --quiet]
	[--reinstall]; [Reinstall the distribution even if you already have the latest version installed]; /icons/perl5language.png; [cpanm --test-only --reinstall]
	[--self-contained]; [Install all non-core modules, even if they're already installed.]; /icons/perl5language.png; [cpanm --test-only --self-contained]
	[--showdeps]; [Only display direct dependencies]; /icons/perl5language.png; [cpanm --test-only --showdeps]
	[--sudo]; [sudo to run install commands]; /icons/perl5language.png; [cpanm --test-only --sudo]
	[--verbose]; [Turns on chatty output]; /icons/perl5language.png; [cpanm --test-only --verbose]
	[ <modules>]; [Install perl modules by name, path or url]; /icons/perl5language.png; [cpanm --test-only <modules>]
