Matching value:
	[--mirror]
Completion items:
	[--auto-cleanup <days>]; [Number of days that cpanm's work directories expire in. Defaults to 7]; /perl5language.png; [cpanm --mirror --auto-cleanup <days>]
	[--force]; [Force install]; /perl5language.png; [cpanm --mirror --force]
	[--from <url>]; [Use only this mirror base URL and its index file]; /perl5language.png; [cpanm --mirror --from <url>]
	[--installdeps]; [Only install dependencies]; /perl5language.png; [cpanm --mirror --installdeps]
	[--interactive]; [Turns on interactive configure (required for Task:: modules)]; /perl5language.png; [cpanm --mirror --interactive]
	[--local-lib <path>]; [Specify the install base to install modules]; /perl5language.png; [cpanm --mirror --local-lib <path>]
	[--local-lib-contained <path>]; [Specify the install base to install all non-core modules]; /perl5language.png; [cpanm --mirror --local-lib-contained <path>]
	[--mirror-only <url>]; [Use the mirror's index file instead of the CPAN Meta DB]; /perl5language.png; [cpanm --mirror --mirror-only <url>]
	[--notest]; [Do not run unit tests]; /perl5language.png; [cpanm --mirror --notest]
	[--prompt]; [Prompt when configure/build/test fails]; /perl5language.png; [cpanm --mirror --prompt]
	[--quiet]; [Turns off the most output]; /perl5language.png; [cpanm --mirror --quiet]
	[--reinstall]; [Reinstall the distribution even if you already have the latest version installed]; /perl5language.png; [cpanm --mirror --reinstall]
	[--self-contained]; [Install all non-core modules, even if they're already installed.]; /perl5language.png; [cpanm --mirror --self-contained]
	[--showdeps]; [Only display direct dependencies]; /perl5language.png; [cpanm --mirror --showdeps]
	[--sudo]; [sudo to run install commands]; /perl5language.png; [cpanm --mirror --sudo]
	[--test-only]; [Run tests only, do not install]; /perl5language.png; [cpanm --mirror --test-only]
	[--verbose]; [Turns on chatty output]; /perl5language.png; [cpanm --mirror --verbose]
	[ <modules>]; [Install perl modules by name, path or url]; /perl5language.png; [cpanm --mirror <modules>]
