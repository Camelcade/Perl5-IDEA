Matching value:
	[--local-lib]
Completion items:
	[--auto-cleanup <days>]; [Number of days that cpanm's work directories expire in. Defaults to 7]; /perl5language.png; [cpanm --local-lib --auto-cleanup <days>]
	[--force]; [Force install]; /perl5language.png; [cpanm --local-lib --force]
	[--from <url>]; [Use only this mirror base URL and its index file]; /perl5language.png; [cpanm --local-lib --from <url>]
	[--installdeps]; [Only install dependencies]; /perl5language.png; [cpanm --local-lib --installdeps]
	[--interactive]; [Turns on interactive configure (required for Task:: modules)]; /perl5language.png; [cpanm --local-lib --interactive]
	[--local-lib-contained <path>]; [Specify the install base to install all non-core modules]; /perl5language.png; [cpanm --local-lib --local-lib-contained <path>]
	[--mirror <url>]; [Specify the base URL for the mirror (e.g. http://cpan.cpantesters.org/)]; /perl5language.png; [cpanm --local-lib --mirror <url>]
	[--mirror-only <url>]; [Use the mirror's index file instead of the CPAN Meta DB]; /perl5language.png; [cpanm --local-lib --mirror-only <url>]
	[--notest]; [Do not run unit tests]; /perl5language.png; [cpanm --local-lib --notest]
	[--prompt]; [Prompt when configure/build/test fails]; /perl5language.png; [cpanm --local-lib --prompt]
	[--quiet]; [Turns off the most output]; /perl5language.png; [cpanm --local-lib --quiet]
	[--reinstall]; [Reinstall the distribution even if you already have the latest version installed]; /perl5language.png; [cpanm --local-lib --reinstall]
	[--self-contained]; [Install all non-core modules, even if they're already installed.]; /perl5language.png; [cpanm --local-lib --self-contained]
	[--showdeps]; [Only display direct dependencies]; /perl5language.png; [cpanm --local-lib --showdeps]
	[--sudo]; [sudo to run install commands]; /perl5language.png; [cpanm --local-lib --sudo]
	[--test-only]; [Run tests only, do not install]; /perl5language.png; [cpanm --local-lib --test-only]
	[--verbose]; [Turns on chatty output]; /perl5language.png; [cpanm --local-lib --verbose]
	[ <modules>]; [Install perl modules by name, path or url]; /perl5language.png; [cpanm --local-lib <modules>]
