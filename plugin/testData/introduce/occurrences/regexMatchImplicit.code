/some<caret>thing${var}else/;
$a =~ /something${var}else/;
m/something${var}else/;
$a =~ m/something${var}else/;
qr/something${var}else/;
$a =~ qr/something${var}else/;

/so mething${var}else/x;
$a =~ /som ething${var}else/x;
m/somethi ng${var}else/x;
$a =~ m/somet   hing${var}else/x;
qr/so mething${var}else/x;
$a =~ qr/some   thing${var}else/x;

/something${var}el
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    se/x;
qr/something${var}el
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    se/x;

/someth ing${var}else/xx;
$a =~ /som  ething${var}else/xx;
m/somet hing${var}else/xx;
$a =~ m/som ething${var}else/xx;
qr/som  ething${var}else/xx;
$a =~ qr/some thing${var}else/xx;

m {somet hing${var}else}xx;
$a =~ m <somet hing${var}else>xx;
qr#somet hing${var}else#xx;
$a =~ qr fso mething${var}elsefxx;


/something${var}else/ix;
$a =~ /something${var}else/ix;
m/something${var}else/ix;
$a =~ m/something${var}else/ix;
qr/something${var}else/ix;
$a =~ qr/something${var}else/ix;

/somethi ng${var}else/;
$a =~ /som ething${var}else/;
m/somethi ng${var}else/;
$a =~ m/som ething${var}else/;
qr/somethi ng${var}else/;
$a =~ qr/some thing${var}else/;

s/something${var}el
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    # commment
    se/asdf/x;
$a =~ s <so mething${var}else>//xx;
s#someth ing${var}else##xx;

