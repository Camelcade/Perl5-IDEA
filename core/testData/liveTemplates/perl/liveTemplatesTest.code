# Live templates test sample
<caret>
{
  say 'hi';
  <caret>
}
<caret>

{
  say 'hi';
  <caret>
} continue {}
<caret>

say 'hi' <caret>;

my $var = 1 + <caret>2;

if($a){}
<caret>

unless($a){}
<caret>

if($a){}
else{}
<caret>

if($a){}
elsif($b){}
<caret>

if($a){}
<caret>
elsif($b){}

if($a){}
<caret>
else{}

while($a){}
<caret>

until($a){}
<caret>

for($a){}
<caret>

foreach($a){}
<caret>

for($a =1;$a < 1; $++){}
<caret>

foreach($a =1;$a < 1; $++){}
<caret>

while($a){}continue{}
<caret>

until($a){}continue{}
<caret>

for($a){}continue{}
<caret>

foreach($a){}continue{}
<caret>

given($a){
  <caret>
}

given($a){
  when($b){}
  <caret>
}

given($a){
  when($b){}
  <caret>
  when($b){}
}

given($a){
  when($b){}
  <caret>
  default{}
}