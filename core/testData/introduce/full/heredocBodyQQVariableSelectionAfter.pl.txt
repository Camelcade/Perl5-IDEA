my $test_name<caret> = "$variable bl";
say <<"EOM";
test${test_name}abla
EOM

say <<EOM;
other test${test_name}abla
EOM

say <<'EOM';
other test$variable blabla
EOM

say <<`EOM`;
test${test_name}abla
EOM

