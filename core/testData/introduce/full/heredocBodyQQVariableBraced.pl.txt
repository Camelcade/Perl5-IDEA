my $test_name<caret> = "test$variable";
say <<"EOM";
test$variable
EOM

say <<EOM;
other test${variable}blabla
EOM

say <<'EOM';
other test${variable}blabla
EOM

say <<`EOM`;
test${variable}other
EOM

