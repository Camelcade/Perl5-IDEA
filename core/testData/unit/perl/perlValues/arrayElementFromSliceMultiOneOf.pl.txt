if( $a ){
  @array = qw/ one two three four/;
  @indexes = (0, -2, 2);
}
else{
  @array = qw/ five six/;
  @indexes = (1);
}

@other = @array[@indexes];

$var = $other[1];

$v<caret>ar;
----------
$var
PsiPerlScalarVariableImpl(Perl5: SCALAR_VARIABLE) at 173 in arrayElementFromSliceMultiOneOf.pl
ArrayItem: Array: [ArraySlice: OneOf: [Array: [five, six], Array: [one, two, three, four]][OneOf: [Array: [0, -2, 2], Array: [1]]]][1]
----------
Resolved
----------
OneOf[five,
three,
undef]